language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t jspeers615/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true jspeers615/docker-react npm run test